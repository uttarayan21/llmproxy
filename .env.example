# Environment Configuration

# Server configuration
HOST=0.0.0.0
PORT=8080

# Database URL (SQLite by default)
DATABASE_URL=sqlite:llmproxy.db

# For development without reverse proxy, you can set a default user
# In production, this should NOT be set - use reverse proxy with Remote-User header
# DEV_DEFAULT_USER=testuser
